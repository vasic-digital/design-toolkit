#!/usr/bin/env bash

# The canonical publication surface for this repository: PUBLIC, and the one
# `main` actually advances on. Matches `git remote get-url origin`.
export UPSTREAMABLE_REPOSITORY="git@github.com:vasic-digital/design-toolkit.git"
